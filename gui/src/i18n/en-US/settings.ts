export default {
  settings: {
    title: 'Settings',
    subtitle: 'Configure KokoroMemo service parameters',
    tabModel: 'Models',
    tabMemory: 'Memory',
    tabStateFiller: 'State Filler',
    tabServer: 'Service',
    tabAdvanced: 'Advanced',
    adv: {
      title: 'Advanced Configuration',
      subtitle: 'Tune the memory system internals; keep defaults if unsure',
      conversation: 'Conversation auto-detection',
      memoryTop: 'Memory master switches',
      scopes: 'Injection scopes',
      extraction: 'Extraction thresholds',
      scoring: 'Scoring weights',
      retrievalGate: 'Retrieval gate',
      hotContext: 'Hot context injection',
      convAutoGap: 'Idle minutes to treat as new session',
      convAutoGapHint: '0 = disabled',
      convDetectPrompt: 'Trigger new session on system prompt change',
      convDetectCount: 'Trigger new session on message count reset',
      injectEnabled: 'Enable memory injection',
      extractionEnabled: 'Enable memory extraction',
      maxRecentTurns: 'Recent turns used for query',
      vectorTopK: 'Vector recall top-k',
      scopeGlobal: 'Inject global memories',
      scopeCharacter: 'Inject character-level memories',
      scopeConversation: 'Inject conversation-level memories',
      extMinImportance: 'Min importance',
      extMinConfidence: 'Min confidence',
      extAfterEachTurn: 'Extract after each turn',
      extFallbackRules: 'Fallback to rules when judge model unavailable',
      scoringHint: 'Recall scoring weights (sum should be ≈ 1.0; current: {total})',
      scoreVector: 'Vector similarity',
      scoreImportance: 'Importance',
      scoreRecency: 'Recency',
      scoreScope: 'Scope',
      scoreConfidence: 'Confidence',
      gateEnabled: 'Enable gate',
      gateMode: 'Mode',
      gateModeAuto: 'Auto',
      gateModeAlways: 'Always retrieve',
      gateModeNever: 'Never retrieve',
      gateModeKeyword: 'Keyword only',
      gateOnNewSession: 'Retrieve on new session',
      gateEveryNTurns: 'Retrieve every N turns',
      gateStateConfBelow: 'Retrieve when state confidence below',
      gateTriggerKeywords: 'Trigger keywords',
      gateSkipCharsBelow: 'Skip when user input chars below',
      gateSkipWhenSufficient: 'Skip when state info is sufficient',
      hcEnabled: 'Enable hot context',
      hcInjectAlways: 'Always inject',
      hcMaxChars: 'Max chars',
      hcSections: 'Sections (toggle + max items per section)',
      hcColEnabled: 'Enabled',
      hcColSection: 'Section',
      hcColMax: 'Max Items',
      section: {
        boundary: 'Boundary',
        scene: 'Scene',
        location: 'Location',
        key_person: 'Key person',
        relationship: 'Relationship',
        main_quest: 'Main quest',
        side_quest: 'Side quest',
        promise: 'Promise',
        open_loop: 'Open loop',
        item: 'Item',
        world_state: 'World state',
        recent_summary: 'Recent summary',
        mood: 'Mood',
        preference: 'Preference',
      },
      help: {
        intro: 'Advanced settings directly tune memory-system internals. Saving writes to config.yaml and takes effect live.',
        conversation: 'Decides when a new session begins. Useful when the upstream client does not reuse conversation_id; enable time-gap or system-prompt-hash detection.',
        memoryTop: 'Master switches. Disabling injection withholds memories from the AI; disabling extraction stops new candidates from being mined.',
        scopes: 'Which scopes contribute to recall: global, character-level, or conversation-level memories.',
        extraction: 'Minimum bar for new candidates. Lower thresholds yield more candidates (and noise). Rule fallback is a safety net when the judge model fails.',
        scoring: 'Per-feature weights for recall scoring. Vector similarity usually dominates; recency may matter less for long-running roleplay.',
        retrievalGate: 'Decides when to actually run vector retrieval. "Auto" combines multiple signals; "Keyword only" retrieves only when triggers appear.',
        hotContext: 'Hot context is a separate injection channel alongside the state board. Each section has its own toggle and item cap.',
      },
      helpDetail: {
        memoryTop: {
          intro: 'Controls whether the memory system runs at all and the basic recall parameters. Disabling injection or extraction effectively takes the memory system offline.',
          inject: 'When off, AI requests will not contain any long-term memory / state board / hot context text. Useful for temporary debugging.',
          extraction: 'When off, conversations will not trigger candidate extraction; the Inbox will receive no new entries. Existing cards remain recallable.',
          recentTurns: 'Number of recent user turns used to build the retrieval query. Too few hurts recall accuracy; too many adds noise. Recommended 4-8.',
          topK: 'Top-k candidates pulled from LanceDB during vector recall. They are then re-scored and filtered into a smaller final set.',
        },
        conversation: {
          intro: 'When the upstream client does not pass an explicit conversation_id (or reuses the same one), the backend can heuristically detect new sessions to avoid mixing conversations.',
          gap: 'If two consecutive requests are this many minutes apart, treat as a new session. 0 disables. Common values: 30-120.',
          prompt: 'A change in the system prompt hash is treated as a new session. Useful for forcing isolation when switching character cards.',
          count: 'A significant drop in message count is treated as a new session. Effective for some tavern-style clients.',
        },
        scopes: {
          intro: 'KokoroMemo categorizes cards into three scopes; this controls whether each scope is included during recall. Disabling all is equivalent to no recall at all.',
          global: 'Globally shared cards (e.g. world settings, general preferences). Recommended to keep enabled.',
          character: 'Cards bound to the current character_id. Disabling skips the character\'s exclusive memories.',
          conversation: 'Temporary memories produced only within the current conversation. Disabling shows only long-accumulated cards.',
        },
        extraction: {
          intro: 'Threshold filters when extracting candidate memories from conversation. Higher = fewer but cleaner candidates; lower = more candidates but noisier.',
          importance: 'Candidates with judge-rated importance below this are dropped. 0.45 is the default balance; lowering captures more day-to-day details.',
          confidence: 'Candidates the judge is unsure about are dropped. Low-confidence memories are prone to factual errors.',
          afterEachTurn: 'When on, extract immediately every turn for fresh memories at the cost of frequent LLM calls. Off lets you trigger externally to save cost.',
          fallback: 'When the judge model times out or errors, fall back to built-in regex rules for basic extraction so candidates are not completely lost.',
        },
        scoring: {
          intro: 'After multi-route recall is merged and deduplicated, results are scored and ranked by these weights. Sum should be ≈ 1.0; higher weight = more influence on ranking.',
          vector: 'Cosine similarity contribution. Recommended to keep highest — it represents semantic match.',
          importance: 'The card\'s own importance field. Increase to favor high-importance cards like "boundary / main quest" in recall.',
          recency: 'Time-decay contribution by card update time. Increase to prefer "recent events"; reduce for long-running roleplay.',
          scope: 'Scope preference (typically conversation > character > global). Increase to let in-session memories outrank global knowledge.',
          confidence: 'The card\'s own confidence field. Low weight barely affects ranking; only matters when confidence gaps are huge.',
        },
        gate: {
          intro: 'The retrieval gate decides whether each request actually queries LanceDB. Fully disabling the gate hugely increases vector DB load and latency.',
          mode: 'Auto = combine multiple signals; Always = retrieve every time; Never = skip entirely; Keyword only = retrieve only when user mentions triggers.',
          newSession: 'Force one retrieval at the start of a new session, so long-term memories get injected into the fresh context.',
          everyN: 'Force a retrieval every N turns. 0 disables, leaving it to other signals.',
          confBelow: 'When the state board overall confidence falls below this threshold (AI lacks current context), trigger a retrieval to fill in.',
          keywords: 'A user message containing any keyword forces retrieval. Common: "remember", "last time", "before".',
          skipChars: 'Skip retrieval when the user input has fewer than this many characters (short messages usually need no info).',
          skipSufficient: 'If the state board already has sufficient context, skip retrieval this turn to save tokens and latency.',
        },
        hotContext: {
          intro: 'Hot context is a fast, rule-based injection channel independent of vector retrieval. It keeps key info like current scene / boundary / main quest always present.',
          always: 'When on, always inject; when off, inject only when the state board lacks info — saves tokens.',
          maxChars: 'Total max characters of hot context. If exceeded, sections are truncated in order — earlier sections are kept first.',
          sections: '14 independent toggles. Each section\'s "max items" controls how many entries to show simultaneously. Typical: boundary 5, key_person 8, main_quest 5, scene 6.',
        },
      },
    },
    helpTitle: 'Help',
    serverConfig: 'Service Configuration',
    openaiBaseUrl: 'OpenAI Base URL',
    openaiBaseUrlHelp: 'Copy this URL into AIRP/SillyTavern or any OpenAI-compatible client.',
    actualPortMismatch: 'Configured port {configured} is unavailable; currently listening on {actual}. Save another port or free the configured port, then restart.',
    localPort: 'Local Listening Port',
    localPortHelp: 'The actual backend listening port. It should match the port shown in the OpenAI Base URL. Click Modify to set a new port; the backend restarts immediately after saving.',
    modifyPort: 'Modify',
    modifyPortTitle: 'Modify Local Listening Port',
    modifyPortConfirm: 'Changing the port saves config and immediately restarts the backend. Current actual listening port: {current}. Make sure your client OpenAI Base URL uses the new port as well.',
    confirmModifyPort: 'Confirm and Restart',
    invalidPort: 'Please enter a port between 1024 and 65535',
    portUnchanged: 'Port unchanged',
    autoSwitchedPort: 'Auto switched',
    storageDir: 'Data Storage Directory',
    storageDirHelp: 'Root directory for all database files. Leave empty for default ./data. Service restarts after change.',
    selectFolder: 'Select Folder',
    timezone: 'Timezone',
    timezoneHelp: 'IANA timezone name, e.g. Asia/Shanghai, America/New_York. Leave empty to use system local time.',
    timezonePlaceholder: 'Use system time',
    language: 'Language',
    languageHelp: 'Interface display language. Takes effect immediately.',
    closeToTray: 'Minimize to Tray on Close',
    updateCheck: 'Check for Updates',
    updateCheckHelp: 'When opening Settings, KokoroMemo reads the update manifest and falls back from GitHub to GitHub Proxy.',
    checkNow: 'Check Now',
    openReleasePage: 'Open Release Page',
    downloadUpdate: 'Download Update',
    updateAsset: 'Matched Asset',
    updateSource: 'Update Source',
    androidUpdateCommand: 'Android One-Click Command',
    updateAvailable: 'New version available',
    noUpdateAvailable: 'You are on the latest version',
    updateCheckFailed: 'Update check failed',
    updateAvailableDetail: 'New version available: current {current}, latest {latest}.',
    noUpdateDetail: 'You are on the latest version: current {current}, latest {latest}.',
    closeToTrayHelp: 'When enabled, the window close button hides the app to the system tray and keeps the backend running. When disabled, closing the window exits KokoroMemo and stops the backend. Disabled by default.',
    llmConfig: 'LLM Configuration',
    forwardTag: 'Requests forwarded to this model',
    forwardHelp: 'Your AIRP client (e.g. SillyTavern) sends requests locally, and KokoroMemo acts as a transparent proxy forwarding conversations to this cloud model while automatically injecting long-term memories and the state board.',
    forwardMode: 'Forward Mode',
    forwardModeHelp: 'Override: use the Key and model configured below (AIRP client can use any Key). Passthrough: use the Key and model from the AIRP client directly.',
    overrideMode: 'Override (use config below, AIRP client Key can be anything)',
    passthroughMode: 'Passthrough (use Key and model from AIRP client)',
    provider: 'Provider',
    providerHelp: 'Select your LLM provider. Most relay services use "OpenAI Compatible".',
    openaiCompatible: 'OpenAI Compatible (DeepSeek/Grok/Relay etc.)',
    baseUrl: 'Base URL',
    baseUrlHelp: 'API endpoint from your provider, no need to add /chat/completions',
    apiKey: 'API Key',
    apiKeyHelp: 'Key from your provider. Saved locally to config.yaml, never uploaded.',
    modelName: 'Model Name',
    modelNameHelp: 'Model ID, e.g. deepseek-chat, gpt-4o, claude-sonnet-4-20250514',
    embeddingConfig: 'Embedding Configuration',
    embeddingHelp: 'Embedding converts text to vectors for semantic retrieval — the core of long-term memory recall. Disabling it means semantic similarity search is unavailable, only keyword matching remains. Default uses ModelArk Qwen3-Embedding-8B, register to get your API Key.',
    enableEmbedding: 'Enable Embedding',
    embeddingApiKeyPlaceholder: 'Embedding service API Key',
    dimension: 'Vector Dimension',
    dimensionHelp: 'Must match the model output dimension. Rebuild index after switching model or dimension.',
    stateFillerConfig: 'State Board Filler Model',
    stateFillerHelp: 'Similar to SillyTavern\'s form-fill feature — uses a cheap, fast model to auto-maintain the multi-tab state board fields (scene, mood, quests, etc.). When disabled, the state board is not auto-updated but can still be edited manually.',
    fillMode: 'Fill Mode',
    fillModeTemplate: 'Template field model fill (Recommended)',
    fillModeRule: 'Legacy rule fill (Compatible)',
    fillModeHelp: 'Recommended: AI only fills user-defined fields and skips locked fields.',
    reuseBaseUrlPlaceholder: 'Reuse memory judge or LLM Base URL if empty',
    reuseApiKeyPlaceholder: 'Reuse memory judge or LLM API Key if empty',
    cheapModelPlaceholder: 'e.g. a cheap fast model',
    reuseModelPlaceholder: 'Reuse memory judge or LLM model if empty',
    minConfidence: 'Min Confidence',
    minConfidenceHelp: 'Each update from the filler model has a confidence score (0~1). Updates below this threshold are skipped. Higher = fewer false updates, lower = more aggressive filling. Default 0.55.',
    timeout: 'Timeout',
    timeoutHelp: 'HTTP request timeout in seconds for the filler model. Increase if the model responds slowly. Default 30s.',
    temperature: 'Temperature',
    temperatureHelp: 'Controls output randomness. 0 = most deterministic, suitable for structured JSON fill tasks. Generally no need to change. Default 0.',
    customPrompt: 'Custom Prompt',
    customPromptHelp: 'Leave empty to use the built-in prompt. Once filled, it completely replaces the built-in prompt — useful for fine-tuning filler behavior.',
    stateFillerPromptPlaceholder: 'Leave empty to use built-in state filler prompt',
    rerankConfig: 'Rerank Configuration',
    rerankHelp: 'Rerank re-sorts recalled memories for better accuracy. Disabled by default, recommended when you have many memories. Default uses ModelArk Qwen3-Reranker-8B.',
    enableRerank: 'Enable Rerank',
    rerankApiKeyPlaceholder: 'Rerank service API Key',
    maxDocsPerBatch: 'Max Docs Per Batch',
    maxDocsHelp: 'Advanced. Max candidates per Rerank request, default 20. Too large may timeout.',
    memoryConfig: 'Memory Configuration',
    memoryHelp: 'Controls AI long-term memory behavior. KokoroMemo uses a three-tier memory architecture: hot memory (state board, injected every turn), warm memory (card graph, recalled on demand), and cold memory (vector index, semantic search). After each turn, noteworthy content is auto-extracted as memory cards. On subsequent conversations, relevant memories are recalled via semantic search and injected into the prompt.',
    enableMemory: 'Enable Memory',
    maxInjectChars: 'Max Inject Chars',
    maxInjectCharsHelp: 'Max total characters of long-term memory cards injected per request. Default 1500. Increasing allows more memory context but consumes the AI\'s context window, reducing space for conversation history.',
    maxRecall: 'Max Recall Count',
    memoryJudge: 'Memory Judge Model',
    memoryJudgeHelp: 'After each turn, uses a cheaper, faster model to decide whether the conversation content is worth saving as a long-term memory card. When disabled, no memory cards are auto-extracted — only raw conversation logging, state board, and manual memory management remain.',
    judgeMode: 'Judge Mode',
    judgeModeModel: 'Model only (Recommended)',
    judgeModeModelRules: 'Model + User Rules',
    judgeModeHelp: '"Model only": fully delegates to the judge model to decide what to remember. "Model + User Rules": appends your custom rules to the prompt, guiding the model to focus on specific content. For example, a rule like "Generate preference when user requests address change" ensures addressing changes are remembered.',
    reuseLlmBaseUrl: 'Reuse LLM Base URL if empty',
    reuseLlmApiKey: 'Reuse LLM API Key if empty',
    reuseLlmModel: 'Reuse LLM model if empty',
    userRules: 'User Rules',
    userRulesHelp: 'One rule per line, only effective in "Model + User Rules" mode. Rules are appended to the judge model\'s prompt to guide extraction. Examples:\n• Generate preference when user requests address change, tags include addressing\n• Generate event when a new character name appears, scope as character\n• Generate boundary when user expresses dissatisfaction, importance 0.8',
    userRulesPlaceholder: 'One rule per line, leave empty for none',
    judgePromptPlaceholder: 'Leave empty to use built-in memory judge prompt',
    rebuildIndex: 'Rebuild Vector Index',
    startMigration: 'Background Async Migration',
    retryVectorSync: 'Retry Failed Vector Syncs',
    vectorMaintenance: 'Vector Index Maintenance',
    rebuildIndexDesc: 'Rebuild LanceDB vector index from approved cards; may take several minutes for large datasets',
    startMigrationDesc: 'After changing embedding model/dimension, asynchronously rebuild index in background while keeping the old index readable — no service interruption',
    retryVectorSyncDesc: 'Batch retry vector sync jobs that failed (e.g. network) — up to 50 per call',
    rebuildIndexConfirm: 'Rebuild vector index? The existing index will be overwritten.',
    startMigrationConfirm: 'Start background async migration? All approved cards will be re-embedded with the current embedding config.',
    migrationStarted: 'Migration started',
    migrationFailed: 'Failed to start migration',
    vectorSyncDone: 'Vector sync: {success} success, {failed} failed (total {total})',
    vectorSyncFailed: 'Vector sync retry failed',
    vectorHelp: {
      intro: 'Vector index maintenance keeps the retrieval system consistent when cards or the embedding config change.',
      rebuild: 'Drop the current LanceDB index entirely and re-embed all approved cards from SQLite. Use only when the index is corrupted or out-of-sync; vector recall is unavailable during the operation.',
      migration: 'Keep the old index serving reads while rebuilding a new index in the background using the current embedding model/dimension. Use when switching embedding model/dimension without service interruption.',
      retry: 'Batch-retry card vector sync jobs that previously failed (e.g. transient network errors) — up to 50 per call. Already-synced cards are unaffected.',
    },
    saveHint: 'Config is saved to config.yaml and takes effect immediately. Service restarts if port or storage directory changes.',
    saveConfig: 'Save Config',
    configSaved: 'Config saved',
    restartingService: 'Config saved, restarting service…',
    serviceRestarted: 'Service restarted',
    autoRestartFailed: 'Auto-restart failed; please restart the service manually',
    migrationStatus: {
      idle: 'Idle',
      running: 'Running',
      completed: 'Completed',
      failed: 'Failed',
    },
    indexRebuilt: 'Index rebuilt: {count} memories indexed',
    rebuildFailed: 'Rebuild failed',
    fetchModelsFailed: 'Request failed, check if backend is running',
    fetchModelsSuccess: 'Fetched {count} models',
    fetchModelsEmpty: 'No models fetched',
    inputBaseUrl: 'Please fill in Base URL first',
    inputApiKey: 'Please fill in API Key first, otherwise cannot fetch remote model list',
    folderDialog: 'Select Data Storage Directory',
    browserEnv: 'Browser debug mode: enter path directly. Desktop version supports folder picker.',
    folderError: 'Cannot open folder picker: {error}',
    folderPermission: 'Please ensure Tauri dialog permission is enabled',
  },
}
